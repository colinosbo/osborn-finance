// Removed: the standalone /welcome onboarding screen was a weaker duplicate of
// the marketing landing (DashboardLanding) and the paid-user connect prompt on
// the Dashboard. Intentionally left as an empty module; no longer routed.
export {};
